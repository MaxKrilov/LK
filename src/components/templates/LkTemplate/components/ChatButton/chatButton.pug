div(
  class="chat-button"
  @click="openChat(); dataLayerPush({ category: 'mainpage', label: 'onlinechat' })"
)
  div(:class="['chat-button__slider', {'slider-animation': isAnimate}]")
    div(class="button") Нужна помощь?
  div(:class="['chat-button__content', {'decrease-animation blink-animation': isAnimate}]")
    er-icon(name="chat")
