div(:class="`${pre}`")
  div(v-show="isLoadingData")
    .stat-template__loading.my-16
      ErtProgressCircular.mr-16(indeterminate width="2")
      .title Загружаем аналитику
  div(v-show="isErrorLoad")
    .stat-template__error.my-16
      .title При загрузке возникла ошибка! Повторите запрос позднее
  div(v-show="!isLoadingData && !isErrorLoad")
    stat-header(
      :title="'Статистика по аудитории'"
      @onPeriodSet="setPeriod"
    )

    er-row(:class="`${pre}__row`")
      er-flex(xs12)
        chart-card(
          :title="'Статистика подключений'"
          :period="period",
          :chartTypes="chartTypes"
          :defaultChartType="'line'"
        )
          template(v-slot:chart)
            connections-chart(
              :data="mock.connections"
              @setLineChartPeriod="e => { periodType = e }"
            )

    div(:class="[`${pre}__row`, 'stat-visitors-grid']")
      // div.stat-visitors-grid__age
        //chart-card(
        //  :title="'Пол/Возраст'"
        //  :period="period",
        //  :defaultChartType="'cluster-bar'"
        //)
        //  template(v-slot:chart)
        //    simple-chart(:data="mock.ages")

      //div.stat-visitors-grid__frequency
      //  chart-card(
      //    :title="'Частота посещений'"
      //    :period="period",
      //    :chartTypes="[chartTypes.bar, chartTypes.pie]",
      //    :defaultChartType="'bar'"
      //  )
      //    template(v-slot:chart)
      //      simple-chart(:data="mock.visits")

      //div.stat-visitors-grid__auth-type
      //  chart-card(
      //    :title="'Тип авторизации'"
      //    :period="period",
      //    :chartTypes="[chartTypes.bar, chartTypes.pie]"
      //    :defaultChartType="'bar'"
      //  )
      //    template(v-slot:chart)
      //      auth-type-chart

    //er-row(:class="`${pre}__row`")
    //  er-flex(xs12 lg6)
    //    chart-card(
    //      :title="'Используемый язык'"
    //      :period="period",
    //      :chartTypes="[chartTypes.bar, chartTypes.pie]"
    //      :defaultChartType="'pie'"
    //    )
    //      template(v-slot:chart)
    //        simple-chart(:data="mock.langs")

    page-footer(:disconnectData="disconnectData")
